# Monad 测试网 RPC（默认已填官方地址，一般无需修改）
MONAD_RPC_URL=https://testnet-rpc.monad.xyz

# 部署用私钥 —— 只写在这个本地文件里，绝对不要发给任何人（包括 AI）！
# 建议：在 MetaMask 新建一个"黑客松专用"测试账户，只往里面充测试网 MON。
DEPLOYER_PRIVATE_KEY=

# 部署成功后填入的合约地址（前端会用到）
CONTRACT_ADDRESS=
